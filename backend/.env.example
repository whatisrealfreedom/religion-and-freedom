# ============================================
# RealFreedom Backend Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.example .env
# NEVER commit .env to version control!

# ============================================
# Server Configuration
# ============================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
ENV=development

# ============================================
# Database Configuration
# ============================================
# Database type: sqlite or postgres
DB_TYPE=sqlite

# SQLite Configuration (when DB_TYPE=sqlite)
DB_PATH=./data/freedom.db

# PostgreSQL Configuration (when DB_TYPE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=freedom
DB_USER=freedom_user
DB_PASSWORD=
DB_SSL_MODE=disable

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins for CORS
# Example: http://localhost:3000,http://localhost:8098,https://yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8098

# ============================================
# JWT Authentication Configuration
# ============================================
# IMPORTANT: Change this to a strong random secret in production!
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=change-me-in-production-use-a-strong-random-secret
JWT_EXPIRY=24h

# ============================================
# Email Configuration (Gmail SMTP)
# ============================================
# Gmail SMTP settings for sending verification emails
# 
# Setup Instructions:
# 1. Enable 2-Step Verification in your Google Account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and your device, then copy the 16-character password
# 4. Use that password for MAIL_PASSWORD below
#
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=your-email@gmail.com
MAIL_FROM_NAME=RealFreedom

# ============================================
# Quick Setup Guide
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and fill in your values
# 3. For Gmail, get an app password (see instructions above)
# 4. For production, generate a strong JWT_SECRET
# 5. Never commit .env to git (it's already in .gitignore)
